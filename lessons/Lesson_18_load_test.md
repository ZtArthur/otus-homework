## Результаты нагрузочного тестирования с помощью NBomber

> Статистика

scenario: `cache_service_load_scenario`

- ok count: `3000`
- fail count: `0`
- all data: `0` MB
- duration: `00:00:30`

load simulations:

- `inject`, rate: `100`, interval: `00:00:01`, during: `00:00:30`

|step|ok stats|
|---|---|
|name|`global information`|
|request count|all = `3000`, ok = `3000`, RPS = `100`|
|latency (ms)|min = `0.88`, mean = `2.12`, max = `27.84`, StdDev = `1.11`|
|latency percentile (ms)|p50 = `1.88`, p75 = `2.36`, p95 = `3.72`, p99 = `5.21`|


> ВЫВОДЫ

- В сравнении с прошлым, отработал хорошо, прирост не значительный, кроме макс. значения (нужно профилировать)

1. Сервис кеша успешно обработал все запросы, без каких либо ошибок.
2. Минимальное время выполнения составило `0.88` мс, а максимальное `27.84` мс.
3. 99% запросов выполнилось быстрее `5.21` мс, медианное составило `1.88`.
4. StdDev `0.81` мс относительно небольшой разброс, что при 100 РПС показывает, что сервис не деградировал.
5. Особое внимание можно уделить max = `27.84`, дополнительно исследовать, возможно влияние GC